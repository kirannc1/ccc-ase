# entity_resolution

Base template: `agents.md` → `B. Agent Template`

- `name`: `entity_resolution`
- `purpose`: Link normalized signals to canonical entities.
- `inputs`: `poc_LLD.md`, `data_architecture.md`, `api_contracts.md`
- `outputs`: entity link draft, validation notes
- `tools`: read-only docs, scoped file write
- `prompt overrides`: resolve identity mappings only; output deterministic JSON only
