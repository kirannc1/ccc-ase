# signal_ingestion

Base template: `agents.md` → `B. Agent Template`

- `name`: `signal_ingestion`
- `purpose`: Capture and shape source signals into structured agent inputs.
- `inputs`: `problem.md`, `poc_LLD.md`, `api_contracts.md`, `data_architecture.md`
- `outputs`: normalized signal payload draft, validation notes
- `tools`: read-only docs, scoped file write
- `prompt overrides`: focus on source-signal extraction, field mapping, and deterministic JSON output only
