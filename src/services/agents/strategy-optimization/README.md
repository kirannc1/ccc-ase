# Agent: Strategy Optimization

Responsibilities:

- multi-objective optimization with constraints
- produce Pareto frontier artifacts + selected option + alternatives
- persist optimizer artifacts for SDA and replay

