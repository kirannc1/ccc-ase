# Agent: Scenario Simulation

Responsibilities:

- run what-if simulations (baseline + interventions)
- compute uncertainty + sensitivity drivers
- persist scenario run outputs for optimization and replay

