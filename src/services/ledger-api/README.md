# `ledger-api`

Append-only ledger service for:

- writing immutable `DecisionLedgerEntry` records
- storing/referencing artifacts (SDA snapshots, evidence packs, scenario runs)
- replay reads (as-of time reconstruction)

